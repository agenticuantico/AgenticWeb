const API=(window.AGENTICUANTICO_API_URL||"https://agenticweb.agenticuantico.workers.dev").replace(/\/$/,"");
const K={chat:"aq_chat_v6",session:"aq_guest_v4",agents:"aq_agents_v3",teams:"aq_teams_v3"};
let history=JSON.parse(localStorage.getItem(K.chat)||"[]"),guest=localStorage.getItem(K.session)||crypto.randomUUID(),agents=JSON.parse(localStorage.getItem(K.agents)||"null")||[
{name:"Asistente",role:"Asistente general",skills:["conversación","organización"],knowledge:["español","productividad"]},
{name:"Programador",role:"Ingeniero de software",skills:["Python","JavaScript","GitHub","debugging"],knowledge:["backend","frontend","APIs","arquitectura"]},
{name:"Diseñador",role:"Diseñador UI/UX y 3D",skills:["UI/UX","3D","branding"],knowledge:["interfaces","responsive","experiencia de usuario"]}],teams=JSON.parse(localStorage.getItem(K.teams)||"[]"),activeAgent=null,activeTeam=null,busy=false,conversation=crypto.randomUUID();
localStorage.setItem(K.session,guest);
const $=id=>document.getElementById(id),esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
function state(x){$("status").textContent=x;$("sideStatus").textContent=x}function toast(x){$("toast").textContent=x;$("toast").classList.add("show");setTimeout(()=>$("toast").classList.remove("show"),2200)}function persist(){localStorage.setItem(K.chat,JSON.stringify(history.slice(-16)))}function add(role,text,save=true){const d=document.createElement("div");d.className="msg "+role;d.innerHTML=role==="assistant"?esc(text).replace(/\n/g,"<br>"):esc(text);$("messages").appendChild(d);$("messages").scrollTop=$("messages").scrollHeight;if(save){history.push({role,content:text});persist()}return d}
async function remote(text){const c=new AbortController(),t=setTimeout(()=>c.abort(),35000);try{const r=await fetch(API+"/v1/public/chat",{method:"POST",signal:c.signal,headers:{"Content-Type":"application/json","X-Guest-Session":guest},body:JSON.stringify({conversation_id:conversation,message:text,consent_to_memory:false,history:history.slice(-10),agent:agents.find(a=>a.name===activeAgent)||null,team:teams.find(a=>a.name===activeTeam)||null})});const d=await r.json().catch(()=>({}));if(!r.ok||!d.answer)throw Error(d.message||"ai_unavailable");return d.answer}finally{clearTimeout(t)}}
async function send(text){if(busy)return;busy=true;$("send").disabled=true;const p=add("assistant","Pensando…",false);add("user",text);state("Conectando con el cerebro…");try{const a=await remote(text);p.innerHTML=esc(a).replace(/\n/g,"<br>");history.push({role:"user",content:text},{role:"assistant",content:a});persist();state(activeTeam?"Equipo conectado · Qwen":activeAgent?activeAgent+" · Qwen":"Cerebro conectado · Qwen")}catch(e){p.textContent="No pude conectar con el cerebro. Intentá nuevamente.";state("Cerebro no disponible");toast("No se pudo conectar con el cerebro")}finally{busy=false;$("send").disabled=false}}
function closeMobile(){$("sidebar").classList.remove("open");$("backdrop").classList.add("hidden")}function showChat(){$("panel").classList.add("hidden");$("chat").classList.remove("hidden");document.querySelectorAll(".nav-item").forEach(x=>x.classList.toggle("active",x.dataset.view==="chat"));$("title").textContent="AgentiCuantico";closeMobile()}
function panel(view){$("chat").classList.add("hidden");$("panel").classList.remove("hidden");document.querySelectorAll(".nav-item").forEach(x=>x.classList.toggle("active",x.dataset.view===view));$("title").textContent=({projects:"Proyectos",agents:"Agentes",teams:"Equipos de trabajo",coder:"Agentic Codex",account:"Registro / perfil",skills:"Skills"})[view];closeMobile();if(view==="agents")agentsPanel();else if(view==="teams")teamsPanel();else if(view==="coder")coderPanel();else if(view==="account")accountPanel();else if(view==="skills")skillsPanel();else projectsPanel()}
function agentsPanel(){$("panel").innerHTML=\`<div class="head"><div><span class="eyebrow">AGENT FACTORY</span><h2>Agentes</h2><p>Creá agentes con nombre, rol, habilidades y conocimientos.</p></div><button class="primary" id="newAgent">＋ Crear agente</button></div><div class="cards">\${agents.map((a,i)=>\`<article class="agent"><div class="avatar">\${esc(a.name[0])}</div><div class="info"><h3>\${esc(a.name)}</h3><span>\${esc(a.role)}</span><p><b>Habilidades:</b> \${a.skills.map(esc).join(" · ")}</p><p><b>Conocimientos:</b> \${a.knowledge.map(esc).join(" · ")}</p></div><button class="ghost use" data-i="\${i}">Abrir chat</button></article>\`).join("")}</div>\`;$("newAgent").onclick=agentForm;document.querySelectorAll(".use").forEach(b=>b.onclick=()=>{activeAgent=agents[+b.dataset.i].name;activeTeam=null;showChat();state("Agente listo · "+activeAgent);toast("Agente seleccionado")})}
function agentForm(){$("panel").innerHTML=\`<div class="head"><div><span class="eyebrow">AGENT FACTORY</span><h2>Crear agente</h2><p>Definí exactamente cómo querés que trabaje.</p></div></div><form id="agentForm" class="card form"><label>Nombre</label><input id="an" required placeholder="Nova"><label>Rol</label><input id="ar" required placeholder="Programador, asistente, diseñador..."><label>Habilidades</label><input id="as" placeholder="Python, GitHub, debugging"><label>Conocimientos</label><input id="ak" placeholder="backend, APIs, arquitectura"><div class="row"><button type="button" class="ghost" id="cancel">Cancelar</button><button class="primary">Crear agente</button></div></form>\`;$("cancel").onclick=()=>panel("agents");$("agentForm").onsubmit=e=>{e.preventDefault();agents.push({name:$("an").value.trim(),role:$("ar").value.trim(),skills:$("as").value.split(",").map(x=>x.trim()).filter(Boolean),knowledge:$("ak").value.split(",").map(x=>x.trim()).filter(Boolean)});localStorage.setItem(K.agents,JSON.stringify(agents));panel("agents");toast("Agente creado")}}
function teamsPanel(){$("panel").innerHTML=\`<div class="head"><div><span class="eyebrow">TEAM WORKSPACE</span><h2>Equipos</h2><p>Un chat conjunto con varios agentes.</p></div><button class="primary" id="newTeam">＋ Crear equipo</button></div><div class="cards">\${teams.length?teams.map((t,i)=>\`<article class="agent"><div class="avatar">◈</div><div class="info"><h3>\${esc(t.name)}</h3><span>\${t.members.length} agentes</span><p>\${t.members.map(esc).join(" · ")}</p></div><button class="ghost useTeam" data-i="\${i}">Abrir chat</button></article>\`).join(""):\`<div class="card">No hay equipos todavía.</div>\`}</div>\`;$("newTeam").onclick=teamForm;document.querySelectorAll(".useTeam").forEach(b=>b.onclick=()=>{activeTeam=teams[+b.dataset.i].name;activeAgent=null;showChat();state("Equipo listo · "+activeTeam);toast("Equipo seleccionado")})}
function teamForm(){$("panel").innerHTML=\`<div class="head"><div><span class="eyebrow">TEAM BUILDER</span><h2>Crear equipo</h2><p>Elegí los agentes que van a colaborar.</p></div></div><form id="teamForm" class="card form"><label>Nombre</label><input id="tn" required placeholder="Producto web"><label>Agentes</label><div class="checks">\${agents.map((a,i)=>\`<label><input type="checkbox" value="\${esc(a.name)}" \${i<2?"checked":""}> \${esc(a.name)}</label>\`).join("")}</div><div class="row"><button type="button" class="ghost" id="tc">Cancelar</button><button class="primary">Crear equipo</button></div></form>\`;$("tc").onclick=()=>panel("teams");$("teamForm").onsubmit=e=>{e.preventDefault();const members=[...document.querySelectorAll(".checks input:checked")].map(x=>x.value);if(!members.length)return toast("Elegí al menos un agente");teams.push({name:$("tn").value.trim(),members});localStorage.setItem(K.teams,JSON.stringify(teams));panel("teams");toast("Equipo creado")}}
function coderPanel(){$("panel").innerHTML=\`<div class="head"><div><span class="eyebrow">AGENTIC CODEX</span><h2>Code Lab</h2><p>Programación, revisión y planificación en un espacio propio.</p></div></div><div class="code-grid"><div class="card form"><label>Agente programador</label><select id="ca">\${agents.map(a=>\`<option>\${esc(a.name)}</option>\`).join("")}</select><label>Objetivo</label><textarea id="cg" placeholder="Ej.: analizá el proyecto y proponé los cambios necesarios."></textarea><button class="primary" id="runCode">Enviar al programador</button></div><div class="card"><h3>Flujo</h3><p>Objetivo → plan → código → revisión → resultado. La ejecución de herramientas se mantiene en backend.</p></div></div>\`;$("runCode").onclick=()=>{const g=$("cg").value.trim();if(!g)return toast("Escribí un objetivo");activeAgent=$("ca").value;showChat();$("input").value=g;$("composer").requestSubmit()}}
function skillsPanel(){$("panel").innerHTML=\`<div class="head"><div><span class="eyebrow">CAPABILITIES</span><h2>Skills</h2><p>Capacidades reutilizables para tus agentes.</p></div></div><div class="skills">\${["Programación","Backend","Frontend","GitHub","Web design","UI/UX","3D","SEO","Datos","Investigación","Automatización","Asistencia"].map(x=>\`<div class="skill"><b>\${x}</b><span>Asignable a cualquier agente</span></div>\`).join("")}</div>\`}
function accountPanel(){$("panel").innerHTML=\`<div class="head"><div><span class="eyebrow">CUENTA</span><h2>Registro / perfil</h2><p>Base visual preparada para autenticación segura.</p></div></div><form id="profile" class="card form"><label>Nombre</label><input id="pn" placeholder="Tu nombre"><label>Email</label><input id="pe" type="email" placeholder="tu@email.com"><button class="primary">Guardar perfil</button></form>\`;$("profile").onsubmit=e=>{e.preventDefault();localStorage.setItem("aq_profile_v3",JSON.stringify({name:$("pn").value,email:$("pe").value}));toast("Perfil guardado")}}
function projectsPanel(){$("panel").innerHTML=\`<div class="head"><div><span class="eyebrow">WORKSPACE</span><h2>Proyectos</h2><p>Separá trabajos y conversaciones.</p></div><button class="primary" id="project">＋ Nuevo trabajo</button></div><div class="card">Creá un trabajo para iniciar una conversación independiente.</div>\`;$("project").onclick=()=>{$("newChat").click()}}
document.querySelectorAll(".nav-item").forEach(b=>b.onclick=()=>b.dataset.view==="chat"?showChat():panel(b.dataset.view));$("mobileMenu").onclick=()=>{$("sidebar").classList.add("open");$("backdrop").classList.remove("hidden")};$("backdrop").onclick=closeMobile;$("menuBtn").onclick=()=>{$("sidebar").classList.contains("open")?closeMobile():$("mobileMenu").click()};$("composer").onsubmit=e=>{e.preventDefault();const t=$("input").value.trim();if(t&&!busy){$("input").value="";send(t)}};$("input").onkeydown=e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();$("composer").requestSubmit()}};$("newChat").onclick=()=>{conversation=crypto.randomUUID();history=[];localStorage.removeItem(K.chat);$("messages").innerHTML="";showChat();state("Cerebro listo")};window.addEventListener("load",()=>{history.forEach(x=>add(x.role,x.content,false));for(let i=0;i<45;i++){const p=document.createElement("i");p.style.setProperty("--x",(Math.random()*260-130)+"px");p.style.setProperty("--y",(Math.random()*260-130)+"px");p.style.setProperty("--z",(Math.random()*260-130)+"px");$("particles").appendChild(p)}});
