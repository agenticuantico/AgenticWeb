const API_BASE=(window.AGENTICUANTICO_API_URL||"https://agenticweb.agenticuantico.workers.dev").replace(/\/$/,"");
const LOCAL_MODEL="onnx-community/Qwen2.5-0.5B-Instruct";
const STORAGE_KEY="aq_chat_v4", SESSION_KEY="aq_guest_session_v2", MAX_HISTORY=12, AGENTS_KEY="aq_agents_v1", TEAMS_KEY="aq_teams_v1";
let generating=false, conversationId=crypto.randomUUID(), activeAgent=null, activeTeam=null;
let history=JSON.parse(localStorage.getItem(STORAGE_KEY)||"[]");
let guestSession=localStorage.getItem(SESSION_KEY)||crypto.randomUUID();
localStorage.setItem(SESSION_KEY,guestSession);\nlet agents=JSON.parse(localStorage.getItem(AGENTS_KEY)||"null")||[{name:"Asistente",role:"Asistente general",skills:["conversación","organización"],knowledge:["español","productividad"]},{name:"Programador",role:"Ingeniero de software",skills:["Python","JavaScript","GitHub","debugging"],knowledge:["backend","frontend","APIs"]},{name:"Diseñador",role:"Diseñador UI/UX y 3D",skills:["UI/UX","3D","branding"],knowledge:["interfaces","responsive","experiencia de usuario"]}];\nlet teams=JSON.parse(localStorage.getItem(TEAMS_KEY)||"[]");\nconst saveWorkspace=()=>{localStorage.setItem(AGENTS_KEY,JSON.stringify(agents));localStorage.setItem(TEAMS_KEY,JSON.stringify(teams))};\nfunction renderPanel(view){const p=$(\"panel\");p.classList.remove(\"hidden\");$(\"chat\").classList.add(\"hidden\");if(view===\"agents\"){p.innerHTML=\"<div class='panel-head'><div><span class='eyebrow'>AGENT FACTORY</span><h2>Agentes</h2><p>Creá agentes con nombre, rol, habilidades y conocimientos.</p></div><button id='createAgent' class='primary'>＋ Crear agente</button></div><div id='agentCards' class='cards'></div>\";fillAgents();$(\"createAgent\").onclick=agentForm}else if(view===\"teams\"){p.innerHTML=\"<div class='panel-head'><div><span class='eyebrow'>WORKSPACE</span><h2>Equipos de trabajo</h2><p>Sumá varios agentes a una conversación conjunta.</p></div><button id='createTeam' class='primary'>＋ Crear equipo</button></div><div id='teamCards' class='cards'></div>\";fillTeams();$(\"createTeam\").onclick=teamForm}else if(view===\"coder\"){p.innerHTML=\"<div class='panel-head'><div><span class='eyebrow'>AGENTIC CODEX</span><h2>Code Lab</h2><p>Entorno propio para trabajar con tu agente programador.</p></div></div><div class='code-layout'><div class='card'><label>Agente</label><select id='coderAgent'>\"+agents.map(x=>\"<option>\"+escapeHtml(x.name)+\"</option>\").join("")+\"</select><label>Objetivo</label><textarea id='codeGoal' placeholder='Ej.: creá una API REST y explicá la estructura.'></textarea><button id='runCodeTask' class='primary wide'>Ejecutar en chat</button></div><div class='card'><b>Flujo Codex</b><p>Objetivo → agente → plan → código → revisión. La ejecución real de herramientas se mantendrá en el backend.</p></div></div>\";$(\"runCodeTask\").onclick=()=>{activeAgent=$(\"coderAgent\").value;const g=$(\"codeGoal\").value.trim();if(!g)return;showChat();$(\"input\").value=g;$(\"composer\").requestSubmit()}}else if(view===\"skills\"){p.innerHTML=\"<div class='panel-head'><div><span class='eyebrow'>CAPABILITIES</span><h2>Skills</h2><p>Capacidades reutilizables para tus agentes.</p></div></div><div class='skill-grid'>\"+[\"Programación\",\"Backend\",\"Frontend\",\"GitHub\",\"Web design\",\"UI/UX\",\"3D\",\"SEO\",\"Datos\",\"Automatización\"].map(x=>\"<div class='skill-card'><b>\"+x+\"</b><span>Disponible para asignar</span></div>\").join("")+\"</div>\"}else if(view===\"account\"){p.innerHTML=\"<div class='panel-head'><div><span class='eyebrow'>CUENTA</span><h2>Registro / perfil</h2><p>Configuración local. No guardamos contraseñas en el navegador.</p></div></div><div class='card form'><label>Nombre</label><input id='profileName' placeholder='Tu nombre'><label>Email</label><input id='profileEmail' type='email' placeholder='tu@email.com'><button id='saveProfile' class='primary'>Guardar perfil</button></div>\";$(\"saveProfile\").onclick=()=>{localStorage.setItem("aq_profile_v1",JSON.stringify({name:$(\"profileName\").value,email:$(\"profileEmail\").value}));setState("Perfil guardado")}}else{p.innerHTML=\"<div class='panel-head'><div><span class='eyebrow'>WORKSPACE</span><h2>Proyectos</h2><p>Espacio para organizar trabajos y conversaciones.</p></div></div><div class='empty-card'>Creá una nueva conversación para iniciar un trabajo.</div>\"}}\nfunction fillAgents(){const r=$(\"agentCards\");r.innerHTML=agents.map((x,i)=>\"<article class='agent-card'><div class='agent-avatar'>\"+escapeHtml(x.name[0]||"A")+\"</div><div class='agent-info'><h3>\"+escapeHtml(x.name)+\"</h3><span>\"+escapeHtml(x.role)+\"</span><p><b>Skills:</b> \"+x.skills.map(escapeHtml).join(" · ")+\"</p><p><b>Conocimientos:</b> \"+x.knowledge.map(escapeHtml).join(" · ")+\"</p></div><div class='agent-actions'><button class='ghost use-agent' data-i='\"+i+\"'>Usar</button></div></article>\").join("");document.querySelectorAll(".use-agent").forEach(b=>b.onclick=()=>{activeAgent=agents[+b.dataset.i].name;activeTeam=null;showChat();setState("Agente seleccionado · "+activeAgent)})}\nfunction fillTeams(){const r=$(\"teamCards\");r.innerHTML=teams.length?teams.map((x,i)=>\"<article class='agent-card'><div class='agent-avatar'>◈</div><div class='agent-info'><h3>\"+escapeHtml(x.name)+\"</h3><span>Equipo</span><p>\"+x.members.map(escapeHtml).join(" · ")+\"</p></div><div class='agent-actions'><button class='ghost use-team' data-i='\"+i+\"'>Abrir chat</button></div></article>\").join(""):\"<div class='empty-card'>No hay equipos. Creá el primero.</div>\";document.querySelectorAll(".use-team").forEach(b=>b.onclick=()=>{activeTeam=teams[+b.dataset.i].name;activeAgent=null;showChat();setState("Equipo seleccionado · "+activeTeam)})}\nfunction agentForm(){const p=$(\"panel\");p.innerHTML=\"<div class='panel-head'><div><span class='eyebrow'>AGENT FACTORY</span><h2>Crear agente</h2></div></div><form id='agentForm' class='card form'><label>Nombre</label><input id='aName' required placeholder='Nova'><label>Rol</label><input id='aRole' required placeholder='Programador, asistente, diseñador...'><label>Habilidades</label><input id='aSkills' placeholder='Python, GitHub, debugging'><label>Conocimientos</label><input id='aKnowledge' placeholder='backend, APIs, arquitectura'><div class='form-row'><button type='button' id='cancelAgent' class='ghost'>Cancelar</button><button class='primary'>Crear agente</button></div></form>\";$(\"cancelAgent\").onclick=()=>renderPanel("agents");$(\"agentForm\").onsubmit=e=>{e.preventDefault();agents.push({name:$(\"aName\").value.trim(),role:$(\"aRole\").value.trim(),skills:$(\"aSkills\").value.split(",").map(x=>x.trim()).filter(Boolean),knowledge:$(\"aKnowledge\").value.split(",").map(x=>x.trim()).filter(Boolean)});saveWorkspace();renderPanel("agents")}}\nfunction teamForm(){const p=$(\"panel\");p.innerHTML=\"<div class='panel-head'><div><span class='eyebrow'>TEAM BUILDER</span><h2>Crear equipo</h2></div></div><form id='teamForm' class='card form'><label>Nombre del equipo</label><input id='tName' required placeholder='Producto web'><label>Agentes</label><div class='checks'>\"+agents.map((x,i)=>\"<label><input type='checkbox' value='\"+escapeHtml(x.name)+\"' \"+(i<2?"checked":"")+\"> \"+escapeHtml(x.name)+\"</label>\").join("")+\"</div><div class='form-row'><button type='button' id='cancelTeam' class='ghost'>Cancelar</button><button class='primary'>Crear equipo</button></div></form>\";$(\"cancelTeam\").onclick=()=>renderPanel("teams");$(\"teamForm\").onsubmit=e=>{e.preventDefault();const m=[...document.querySelectorAll(".checks input:checked")].map(x=>x.value);if(!m.length)return;teams.push({name:$(\"tName\").value.trim(),members:m});saveWorkspace();renderPanel("teams")}}\nfunction showChat(){$(\"panel\").classList.add("hidden");$(\"chat\").classList.remove("hidden");document.querySelectorAll(".nav-item").forEach(x=>x.classList.toggle("active",x.dataset.view==="chat"))}\ndocument.querySelectorAll(".nav-item").forEach(b=>b.dataset.view&&(b.onclick=()=>{document.querySelectorAll(".nav-item").forEach(x=>x.classList.toggle("active",x===b));if(b.dataset.view==="chat")showChat();else renderPanel(b.dataset.view)}));$(\"menuBtn\").onclick=()=>renderPanel(activeAgent||activeTeam?\"account\":\"account\");
const $=id=>document.getElementById(id);
function setState(text){$("status").textContent=text;$("sideStatus").textContent=text;}
function persist(){history=history.slice(-MAX_HISTORY);localStorage.setItem(STORAGE_KEY,JSON.stringify(history));}
function add(role,text,save=true){
 const d=document.createElement("div"); d.className="msg "+role;
 d.innerHTML=role==="assistant"?format(text):escapeHtml(text);
 $("messages").appendChild(d); $("messages").scrollTop=$("messages").scrollHeight;
 if(save){history.push({role,content:text});persist()} return d;
}
function escapeHtml(s){return String(s).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));}
function format(s){return escapeHtml(s).replace(/\n/g,"<br>");}
async function askRemote(text){
 const controller=new AbortController(); const timer=setTimeout(()=>controller.abort(),32000);
 try{
  const r=await fetch(API_BASE+"/v1/public/chat",{method:"POST",signal:controller.signal,headers:{"Content-Type":"application/json","X-Guest-Session":guestSession},
   body:JSON.stringify({conversation_id:conversationId,message:text,consent_to_memory:false,history:history.filter(x=>x.role==="user"||x.role==="assistant").slice(-10),agent:agents.find(x=>x.name===activeAgent)||null,team:teams.find(x=>x.name===activeTeam)||null})});
  if(!r.ok)throw Error("remote"); const d=await r.json(); if(!d.answer)throw Error("empty"); return d.answer;
 }finally{clearTimeout(timer)}
}
async function ask(text){const a=await askRemote(text);setState("Cerebro conectado · Qwen");return a}
async function send(text){
 if(generating)return; generating=true; const p=add("assistant","Pensando…",false); add("user",text);
 try{const a=await ask(text);p.innerHTML=format(a);history.push({role:"user",content:text},{role:"assistant",content:a});persist()}
 catch(_){p.textContent="No pude responder ahora. Intentá nuevamente en unos segundos."}
 finally{generating=false}
}
$("composer").addEventListener("submit",async e=>{e.preventDefault();const i=$("input"),t=i.value.trim();if(!t||generating)return;i.value="";await send(t)});
$("newChat").addEventListener("click",()=>{conversationId=crypto.randomUUID();history=[];localStorage.removeItem(STORAGE_KEY);$("messages").innerHTML="";});
$("input").addEventListener("keydown",e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();$("composer").requestSubmit()}});
function initParticles(){
 const root=$("particles");
 for(let i=0;i<54;i++){const p=document.createElement("i"),a=Math.random()*Math.PI*2,b=Math.acos(2*Math.random()-1),r=118+Math.random()*35;
  p.style.transform=`translate3d(${Math.sin(b)*Math.cos(a)*r}px,${Math.sin(b)*Math.sin(a)*r}px,${Math.cos(b)*r}px)`;
  p.style.animationDelay=`-${Math.random()*5}s`;root.appendChild(p)}
}
window.addEventListener("load",()=>{initParticles();setState("Cerebro listo");});