# AgentiCuantico Android APK

The Android build is produced by `.github/workflows/android-apk.yml` and uploaded as the `AgentiCuantico-Android-debug` workflow artifact.

GitHub Actions artifacts are downloadable from the workflow run after the build completes.
